name: tes
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.19.0
  firebase_core: ^3.6.0
  cloud_firestore: ^5.4.4

dev_dependencies:
  flutter_test:
    sdk: flutter
firebase_core: latest_version
firebase_database: latest_version

flutter:
  uses-material-design: true
  assets:
    - assets/fitness-1.jpg
    - assets/fitness-2.jpg
    - assets/fitness-3.jpg
